---
# vars file for infra_server_kvm_ansible_bootstrap

# VM Directories
vm_root_dir: /home/ppetrou/VirtualMachines
vm_keys_dir: "{{ vm_root_dir }}/keys"

# VM Files
vm_root_disk_device: "vda"
vm_root_disk_device_format: "qcow2"

